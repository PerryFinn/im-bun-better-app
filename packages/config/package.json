{
  "name": "@im-debug-better-app/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean:build": "rimraf dist build .tsbuildinfo",
    "clean:node_modules": "rimraf node_modules",
    "clean:turbo": "rimraf .turbo"
  }
}
